import React from 'react';
import { Container, Row, Col, Button } from 'react-bootstrap';

const SuperPlan = () => {
  return (
    <Container>
      <Row>
        <Col>
          <p>This is I forget the name..</p>
          <Button variant="primary">Get Started</Button>
        </Col>
      </Row>
    </Container>
  );
};

export default SuperPlan;